import Typography from '@mui/material/Typography';

export const metadata = { title: 'Notifikacie | Snap-zoska'};

export default function Notifications() {
  return (

      <Typography> Notifikacie </Typography>

  );
}