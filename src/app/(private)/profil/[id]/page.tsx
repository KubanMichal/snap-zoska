import Typography from '@mui/material/Typography';

export const metadata = { title: 'Detail profilu | Snap-zoska'};

export default function ProfileDetail() {
  return (

      <Typography> Detail profilu </Typography>

  );
}